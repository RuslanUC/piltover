from piltover.tl{namespace} import {objects}
from piltover.tl.converter import ConverterBase


class {base}Converter(ConverterBase):
    base = {base}
    old = [{old}]
    layers = [{layers}]

{methods}